FROM kszucs/miniconda3

RUN apk --no-cache add git libgit2-dev build-base \
 && conda install -y cffi \
 && pip --no-cache-dir install pygit2==0.24.1 \
 && conda clean -y -a \
 && rm -rf /var/cache/apk/* \
 && apk del --purge build-base
